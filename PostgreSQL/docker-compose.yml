services:
  postgres:
    container_name: postgres
    image: postgres:latest
    ports:
      - "5432:5432"
    env_file:
      - .env.local
    restart: no
    volumes:
      - volume:/var/lib/postgresql/data
volumes:
  volume:
